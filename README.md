## Usage

```bash
git clone git@github.com:TouhouFishClub/baibai-qq-channel-bot.git
git submodule update --init --recursive

# 安装ts-node
npm install -g ts-node
npm install

# 配置src/.secret.json （参照.secret.example.json）

# 运行
ts-node ./src/index.ts
```